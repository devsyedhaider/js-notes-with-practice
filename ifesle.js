/* 1. Check number is positive or negative

Input: 5 → “Positive”
Input: -3 → “Negative” 

let num = prompt("Enter a number : ");

if (num > 0) {
  console.log("Your Number is Positive .");
} else if (num < 0) {
  console.log("Your Number is negitive");
}

*/

/* 

2. Check user age for voting

Agar age ≥ 18 → “You can vote”
warna → “You cannot vote”

let age = prompt(
  "Enter Your age I will check your age and respones you ! you are eligible or not eligible to vote  : "
);

if (age >= 18) {
  console.log("You are eligible to Vote.");
} else if (age < 18) {
  console.log("Sorry ! You cannot vote");
}

*/

/* 

Traffic light system

Input "red", "yellow", "green"
Output:

red → Stop

yellow → Ready

green → Go


let ligth = prompt("Enter current light color : ");

if (ligth === "red") {
  document.body.style.background = "red";
} else if (ligth === "yellow") {
  document.body.style.background = "yellow";
} else if (ligth === "green") {
  document.body.style.background = "green";
}

*/
